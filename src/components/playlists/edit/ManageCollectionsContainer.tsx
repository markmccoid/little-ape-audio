import { View, Text } from "react-native";
import React, { useState } from "react";
import ManageCollectionsAnim from "./ManageCollectionsAnim";

const ManageCOllectionsContainer = () => {
  // const color = useTempStore((state) => state.tmpCollectionColor);
  // const textColor = getTextColor(getColorLuminance(color).colorLuminance);

  return <ManageCollectionsAnim />;
};

export default ManageCOllectionsContainer;
