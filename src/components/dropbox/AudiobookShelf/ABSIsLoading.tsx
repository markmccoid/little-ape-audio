import { View, Text, ActivityIndicator } from "react-native";
import React from "react";

const ABSIsLoading = () => {
  return (
    <View className="mt-3">
      <ActivityIndicator size="large" />
    </View>
  );
};

export default ABSIsLoading;
